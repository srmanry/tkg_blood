import 'package:flutter/material.dart';

class ANagativeScreen extends StatelessWidget {
 const ANagativeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("B - Nagative "),),
    );
  }
}
