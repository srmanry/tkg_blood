import 'package:flutter/material.dart';

class ONegativeScreen extends StatelessWidget {
  const ONegativeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("O - Nagative "),),
    );
  }
}
