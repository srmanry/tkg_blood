import 'package:flutter/material.dart';

class AbNagativeScreen extends StatelessWidget {
 const AbNagativeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("AB - Nagative "),),
    );
  }
}
